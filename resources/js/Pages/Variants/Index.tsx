import{Head,Link,router}from'@inertiajs/react';import{useState,type FormEvent}from'react';import AuthenticatedLayout from'../../Layouts/AuthenticatedLayout';
type V={id:string;name:string;sku:string|null;barcode:string|null;currency:string;status:string;price:string;available:number;listings_count:number;product:{id:string;name:string}};type P={data:V[];links:{url:string|null;label:string;active:boolean}[]};
export default function Index({variants,filters}:{variants:P;filters:{search:string}}){const[q,setQ]=useState(filters.search);const search=(e:FormEvent)=>{e.preventDefault();router.get('/variants',{search:q},{preserveState:true})};return <AuthenticatedLayout><Head title="Varyant Listesi"/><section className="page-heading heading-actions"><div><span className="eyebrow">Katalog</span><h1>Varyant Listesi</h1><p>Tüm satılabilir ürün varyantlarını, stoklarını ve kanal bağlantılarını tek ekranda inceleyin.</p></div></section><section className="panel"><form className="search-form" onSubmit={search}><input placeholder="Ürün, varyant, SKU veya barkod ara" value={q} onChange={e=>setQ(e.target.value)}/><button className="button">Ara</button></form><div className="table-wrap"><table><thead><tr><th>Ürün / Varyant</th><th>SKU</th><th>Barkod</th><th>Fiyat</th><th>Satılabilir stok</th><th>Kanallar</th><th>Durum</th></tr></thead><tbody>{variants.data.map(v=><tr key={v.id}><td><Link href={`/products/${v.product.id}`}><strong>{v.product.name}</strong><br/><small>{v.name}</small></Link></td><td>{v.sku??'—'}</td><td>{v.barcode??'—'}</td><td>{v.price} {v.currency}</td><td>{v.available}</td><td>{v.listings_count}</td><td><span className="status">{v.status}</span></td></tr>)}</tbody></table>{variants.data.length===0&&<div className="empty-state"><h3>Varyant bulunamadı</h3><p>Aramayı değiştirin veya yeni ürün oluşturun.</p></div>}</div></section></AuthenticatedLayout>}
